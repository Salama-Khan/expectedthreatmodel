# Screenshots

Drop UI captures here so the root README can embed them:

- `match.png` — Match view with the interactive pitch
- `players.png` — player impact leaderboard
- `model.png` — global xT surface

These files are intentionally not committed until you capture them from a running app.
